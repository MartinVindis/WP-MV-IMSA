////////////////////////////// CAMERA AND GRAPHICAL EFFECTS ///////////////////////////////////////////

Eyepoint=(-0.189, 0.801, -0.265)      // cockpit eyepoint (left/right, up/down, front/back)
CameraTarget=(0.000, 0.850, 0.350)  // target for all other cameras
TVOffset=(0.0,0.0,0.0)		// NEW additional offset for tv cockpit view (defaults to 0,0,0)
MirrorPos=(0.0,0.90,0.4)		// Mirror position
FrontWheelsInCockpit=1
CockpitVibrationMag=0.006

// Variables to control the helmet movement as seen from the outside
HeadMaxPos=(0.015,0.007,0.007)  // default is (0.06,0.02,0.02),first number is left-to-right movement with the head roll
HeadMaxOri=(0.025,0.025,0.075)  // default is (0.10,0.10,0.30),last number is head roll

////////////////////////////// COCKPIT GAUGE SETTINGS///////////////////////////////////////////////
/////All the fonts are assumed to be .BMP, but if you want to change that just include       
////the extension, for example, FONTMONKEY.TGA.                                  ////////////////////////////////////////////////////////////////////////////////////////////////////
////////////DIGITAL GAUGES/////////////////////

RPMLED=HPDBAR

LCDFont=HPD_FONTLCD.BMP			// defaults to FONTREAD
LCDBackground=HPD_LCDBKG       // default is "READOUT.BMP"
LCDScale=(1.0, 1.6)         // how big to draw the font onto the background

GearsFont=DDU310_DIGIFONTW.TGA			// defaults to FONTREAD
GearsBackground=DDU310_GEARSBKG       // default is "READOUT.BMP"
GearsScale=(1.8,2.4)         // how big to draw the font onto the background

SpeedFont=DDU310_DIGIFONTY.TGA     // defaults to FONTREAD
SpeedBackground=DDU310_SPEEDBKG      // defaults to READOUT.BMP
SpeedScale=(3.0,2.0)          // how big to draw the font onto the background

PlaceFont=DDU310_DIGIFONTW.TGA      // defaults to FONTREAD
PlaceBackground=DDU310_PLACEBKG      // defaults to READOUT.BMP
PlaceScale=(1.5,2.5)          // how big to draw the font onto the background